# متطلبات نظام إدارة متجر الأدوات الكهربائية

# المتطلبات الأساسية (مدمجة مع Python)
# tkinter - واجهة المستخدم الرسومية
# sqlite3 - قاعدة البيانات
# datetime - التعامل مع التواريخ والأوقات
# threading - المعالجة المتوازية
# shutil - عمليات الملفات
# os - عمليات نظام التشغيل

# المتطلبات الاختيارية (تحسن من تجربة المستخدم)
matplotlib>=3.5.0          # للرسوم البيانية في التقارير
tkcalendar>=1.6.0          # لاختيار التواريخ بسهولة
Pillow>=8.0.0              # لمعالجة الصور
cryptography>=3.4.0       # لتشفير النسخ الاحتياطية

# متطلبات التطوير (اختيارية)
pytest>=6.0.0             # لاختبار الوحدات
black>=21.0.0              # لتنسيق الكود
flake8>=3.9.0              # لفحص جودة الكود

# ملاحظات التثبيت:
# 1. لتثبيت المتطلبات الأساسية فقط:
#    pip install matplotlib tkcalendar Pillow cryptography

# 2. لتثبيت جميع المتطلبات:
#    pip install -r requirements.txt

# 3. في حالة مشاكل التثبيت على Windows:
#    python -m pip install --upgrade pip
#    pip install -r requirements.txt

# 4. للتشغيل بدون المتطلبات الاختيارية:
#    يمكن تشغيل النظام بدون matplotlib و tkcalendar
#    لكن بعض الميزات ستكون محدودة
